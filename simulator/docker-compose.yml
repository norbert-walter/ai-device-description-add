services:
  add-simulator:
    build: .
    image: add-simulator:1.0.1
    container_name: add-simulator
    ports:
      - "${PORT:-5000}:5000"
    environment:
      - PORT=5000
      - GUNICORN_WORKERS=1
    restart: unless-stopped
